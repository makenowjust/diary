doctype html

html(lang='ja')
  head
    meta(charset='utf-8')
    block title
      title= `${typeof title === 'undefined' ? '' : `${title} - `}$ make now just`
    link(rel='stylesheet', href=`${baseUrl}/css/index.css`)
  body
    header
      h1
        a(href=baseUrl) $ make now just
    main
      block main
    footer
      small &copy; 2016 TSUYUSATO "MakeNowJust" Kitsune
